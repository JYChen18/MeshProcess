root: assets/example_obj

input_template: ${data.root}/raw_mesh/**

output_template: ${data.root}/processed_data/**

output_vision_template: ${data.root}/vision_data/**

output_split: ${data.root}/valid_split
